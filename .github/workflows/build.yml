name: Build Mobile App

on:
  workflow_dispatch:
    inputs:
      platform:
        description: 'Select platform to test'
        required: true
        default: 'both'
        type: choice
        options:
          - ios
          - android
          - both
      android_type:
        description: 'Android build type'
        required: true
        default: 'both'
        type: choice
        options:
          - aab
          - apk
          - both
      ios_type:
        description: 'iOS build type'
        required: true
        default: 'both'
        type: choice
        options:
          - bundle
          - simulator
          - both

      build_id:
        description: 'Internal build ID'
        required: true
        type: string
      node_version:
        description: 'Node.js version'
        required: false
        default: '24.11.1'
      java_version:
        description: 'Java version'
        required: false
        default: '17'
      ruby_version:
        description: 'Ruby version'
        required: false
        default: '3.3'
      api_url:
        description: 'API URL'
        required: true


jobs:
  mobile-builds:
    name: Mobile Builds
    uses: appnatively/workflows/.github/workflows/action.yml@master
    secrets:
      APPNATIVELY_SECRETS: ${{ secrets.APPNATIVELY_SECRETS }}
    with:
      build_id: ${{ github.event.inputs.build_id }}
      platform: ${{ github.event.inputs.platform }}
      android_type: ${{ github.event.inputs.android_type }}
      ios_type: ${{ github.event.inputs.ios_type }}
      node_version: ${{ github.event.inputs.node_version }}
      java_version: ${{ github.event.inputs.java_version }}
      ruby_version: ${{ github.event.inputs.ruby_version }}
